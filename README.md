# kasandra_backened_repo
repo for python pipeline

### Clone the repo ` http `


### Run fastapi ` pip install "fastapi[standard]" `
### Run dependency ` pip install -r requirements.txt `
### Run the Application ` uvicorn app.main:app --reload `

#### Example `` kasadra_backened_repo\learning_app> pip install -r requirements.txt ``
#### Example `` kasadra_backened_repo\learning_app> uvicorn main:app --reload ``