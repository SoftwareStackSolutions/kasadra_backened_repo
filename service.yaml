apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: kasadara
spec:
  type: ClusterIP
  selector:
    app: python-application
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000



