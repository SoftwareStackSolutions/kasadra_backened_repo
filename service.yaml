apiVersion: v1
kind: Service
metadata:
  name: python-application-service
  namespace: kasadara
spec:
  type: ClusterIP
  selector:
    app: python-application
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8000



