apiVersion: v1
kind: Service
metadata:
  name: python-application-service
  namespace: kasadara
spec:
  type: LoadBalancer ## ClusterIP
  selector:
    app: python-application
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
