version: 2
updates:
  # 1. Keep Python dependencies updated (e.g., requirements.txt)
  - package-ecosystem: "pip"
    directory: "/"  # location of requirements.txt
    schedule:
      interval: "weekly"  # options: daily, weekly, monthly
    open-pull-requests-limit: 5
    commit-message:
      prefix: "deps(pip)"
